// Re-export from domain — this file is kept for backward compatibility
export { usePermissions, useHasPermission, useHasRole } from '@/domains/identity/hooks/use-permissions';
export type { AppRole, AppPermission } from '@/domains/identity/hooks/use-permissions';
