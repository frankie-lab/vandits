// Re-export from domain — this file is kept for backward compatibility
export { useAuth } from '@/domains/identity/hooks/use-auth';
export type { UserProfile } from '@/domains/identity/hooks/use-auth';
