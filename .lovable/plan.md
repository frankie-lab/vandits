Eliminar el atributo `title="Mis puntos en Catálogo — filtros rápidos"` del `<button>` de `PopoverTrigger` en `src/components/toolbar/MyCatalogQuickFilters.tsx` (línea 196).

Es el tooltip nativo del navegador que aparece al hacer hover sobre el contador verde de "Mis puntos" en `FloatingToolbar`. Sin reemplazo (el popover ya explica el contexto al hacer click).

Cambio mínimo, una línea, sin tocar lógica ni estilos.